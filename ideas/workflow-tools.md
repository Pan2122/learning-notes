请基于 `PROJECT_ARCHITECTURE.md` (或当前项目上下文)，帮我执行“新建笔记”的自动化流程。

## 1. 输入变量 (用户填写区)

请读取以下我填写的示例变量，并据此执行任务：

* ​**核心类别**​: [ projects ]  <-- 选项: c-lang / hardware / projects / ideas
* ​**英文文件名**​: [ mpu6050-driver ] <-- 不要带后缀
* ​**中文标题**​: [ MPU6050 姿态解算与驱动开发 ]
* ​**文章描述**​: [ 基于 STM32 的 I2C 驱动实现与卡尔曼滤波应用 ]
* ​**标签**​: [ STM32, I2C, 传感器 ]
* ​**是否需要图片目录**​: [ 是 ]

---

## 2. 执行任务指令 (Agent Execution)

### 任务 A：创建 Markdown 文件

在 `{核心类别}/` 目录下创建 `{英文文件名}.md`。 文件内容必须包含以下 Frontmatter 和结构（请保持 YAML 格式正确）：

Markdown

```
---
layout: doc
title: {中文标题}
description: {文章描述}
tags: [{标签}]
---

# {中文标题}

> 简介：{文章描述}

## 1. 背景
(此处留空)

## 2. 实现细节
(此处留空)
```

### 任务 B：注册侧边栏 (Critical)

读取 `.vitepress/config.mts`。 找到 `sidebar` 配置中 `{核心类别}/` 对应的部分。 在 `items` 数组的**末尾**追加一个新的对象：`{ text: '{中文标题}', link: '/{核心类别}/{英文文件名}' }`

*注意：不要删除或修改现有的其他侧边栏条目，仅做追加操作。*

### 任务 C：创建图片资源目录 (如果上方选了“是”)

1. 在 `public/images/{核心类别}/{英文文件名}/` 创建一个空文件夹。
2. 请告诉我未来在 Markdown 中引用图片的路径格式。
   * 示例格式：`/images/{核心类别}/{英文文件名}/xx.png`

---

请一步步执行上述操作，并在完成后确认侧边栏配置正确。
