
---

### 一、 工程与集成元件库 (Project & Library)

在开始设计前，良好的文件管理习惯能帮你规避掉50%的潜在错误。

#### ⚠️ 核心注意事项

1. **工程文件 (.PrjPcb) 是核心**：永远不要单独打开一个 `.SchDoc` 或 `.PcbDoc` 进行设计。必须先新建 `Project`，将所有文件（原理图、PCB、库）拖入工程并**保存工程**，否则原理图和PCB之间无法进行数据同步（Update）。
2. **路径管理**：尽量不要使用中文路径，避免莫名其妙的报错。
3. **库的统一性**：建议使用集成库（Integrated Library），或者确保原理图库（.SchLib）与封装库（.PcbLib）在同一工程目录下，并正确关联 Footprint（封装）。

#### ⌨️ 常用快捷键 (库编辑)

| 功能 | 快捷键 | 说明 |
| --- | --- | --- |
| **新建元件/封装** | **T** + **C** | 在库列表里快速创建新条目 |
| **重命名** | **F2** | 选中元件名重命名 |
| **设置参考点** | **E** + **F** + **C** | 在PCB库中，将原点设置到引脚中心 (Edit -> Set Reference -> Center) |
| **测量距离** | **Ctrl** + **M** | 极其常用，测量焊盘间距 |
| **清除测量** | **Shift** + **C** | 清除屏幕上的测量标记 |

---

### 二、 原理图绘制 (Schematic)

这是逻辑设计的核心，你之前问过的 **N (Net Label)** 就是在这里最关键。

#### ⚠️ 核心注意事项

1. **栅格捕捉 (Grid Snap) 是生命线**：
* **切记**：绘制原理图时，捕捉栅格（Snap Grid）**必须**设为 **100mil** (或者至少 50mil)。
* **后果**：如果设为 1mil 或其他非标准值，肉眼看导线是连上的，但系统判定为“未连接”（开路），导入PCB时会丢失网络。


2. **网络标签 (Net Label) vs 端口 (Port)**：
* **Net Label**：用于同一张原理图内的电气连接。
* **Port**：用于多张原理图（层次原理图）之间的信号连接。


3. **统一标注**：画完图后，务必使用 `Tools` -> `Annotation` -> `Annotate Schematics Quietly` 自动对元件编号（如 R1, R2...），不要有问号（R?）。

#### ⌨️ 高频快捷键 (原理图)

| 功能 | 快捷键 | 备注 |
| --- | --- | --- |
| **放置导线** | **P** + **W** | Place Wire，最常用的连线命令 |
| **放置网络标签** | **P** + **N** | Place Net Label，即你常提到的 **N** 键，给线起名字 |
| **放置电源/地** | **P** + **O** | Place Power Port (GND/VCC) |
| **旋转元件** | **Space** (空格) | 逆时针旋转90度 |
| **镜像元件** | **X** 或 **Y** | 拖动元件时按 X (左右镜像) 或 Y (上下镜像) |
| **复制/粘贴** | **Shift** + **拖动** | 按住 Shift 拖动元件 = 快速复制 |
| **查找元件** | **Ctrl** + **F** | 在原理图中搜索特定的元件号或网络名 |
| **高亮网络** | **Alt** + **点击连线** | 查看该网络连接到了哪些地方 |

---
你说得很对，刚才那份确实太“入门”了，对于实战 Layout 来说，仅仅知道 `P+T` 是远远不够的。Layout 工程师的效率往往体现在对**布线模式切换**、**铺铜管理**和**批量选择**的熟练度上。

这份是 **进阶版/实战级** PCB Layout 快捷键补全，特别针对你提到的**走线**和**铺铜**做了深度展开，都是老手画板时手不停敲的高频键。

---

### 三、 走线 (Routing) —— 核心中的核心

仅仅知道 `P`+`T` 开始布线是不够的，真正的技巧在于**布线过程中的控制**。

**启动指令：**

* **P** + **T**：交互式布线 (Interactive Routing) —— **最常用**。
* **P** + **M**：多根走线 (Multi-Route) —— 同时拉总线用。
* **P** + **D** + **L**：差分对布线 (Differential Pair Routing)。

**🔴 走线过程中的“神技” (按键在布线状态下生效)：**

| 快捷键 (布线中) | 功能描述 | **老手经验 (必看)** |
| --- | --- | --- |
| **Shift** + **R** | **切换布线模式** (推挤/环绕/停止) | 遇到障碍物时，是**推开它 (Push)**，还是**抱紧它 (Hug)**，还是**停住 (Stop)**。这是Layout最常用的键之一。 |
| **Shift** + **Space** | **切换转角模式** | 在 45°角、圆弧、90°直角、任意角度之间切换。画蛇形线或高频线常切到圆弧模式。 |
| **Space** (空格) | **切换转角方向** | 改变走线的起始弯折方向。 |
| **BackSpace** (退格) | **撤销上一步** | **千万别按 Esc**，Esc是退出布线。画错了按 BackSpace 回退一节。 |
| **Shift** + **W** | **选择线宽** | 弹窗选择预设的线宽列表。 |
| **3** | **循环改变线宽** | 在规则设定的 Min / Preferred / Max 线宽之间循环切换。 |
| **Ctrl** + **点击** | **自动完成走线** | 如果路径简单，按住Ctrl点终点，软件会自动帮你把剩下的线连上 (Auto Route path)。 |
| **`** (波浪号) | **显示快捷菜单** | 记不住按键时，布线途中按这个，会弹出当前能用的所有操作列表。 |
| **Ctrl** + **Shift** + **滚轮** | **换层并打孔** | 布线时直接切换图层，软件会自动在末端加一个过孔。 |

---

### 四、 铺铜与电源平面 (Polygon & Planes)

你特别提到的铺铜，AD里分“多边形铺铜(Polygon)”和“内电层(Plane)”。

**基础指令：**

* **P** + **G**：**放置多边形铺铜** (Place Polygon Pour) —— **核心键**。
* **P** + **Y**：**放置铺铜挖空区域** (Polygon Pour Cutout) —— 在铺铜里挖个洞，比如天线区域净空。
* **P** + **S**：**放置铺铜分割线** (Slice Polygon Pour) —— 把一块大铜皮切成两块。

**🔴 铺铜管理 (这也非常重要，否则卡死)：**

| 快捷键 | 功能描述 | **老手经验** |
| --- | --- | --- |
| **T** + **G** + **A** | **重铺所有铜皮** (Repour All) | 改完规则或动了线后，铜皮会变绿报错，必须用这个刷新。**肌肉记忆键**。 |
| **T** + **G** + **H** | **搁置(隐藏)铜皮** (Shelve) | **强推！** 修改复杂板子时，铺铜会挡视线且拖慢显卡。先 Shelve 把它“收起来”变成只有框，改完板子再 Unshelve。 |
| **T** + **G** + **E** | **恢复铜皮** (Unshelve) | 把刚才“收起来”的铜皮放出来。 |
| **Ctrl** + **D** | **查看/隐藏铺铜** | 在 View Options 里可以设置 Polygon 是显示 Fill (实心) 还是 Draft (轮廓)。 |

---

### 五、 高效选择与修改 (Selection) —— 像做手术一样精准

有时候想删一根线，结果把整个网络删了；或者想选整个网络，却只选了一段。这里是解药：

* **S** (菜单)：弹出选择菜单 (Select)，是一切选择操作的入口。

| 快捷键 | 功能描述 | **场景举例** |
| --- | --- | --- |
| **Tab** | **选中连接的导线** | 点中一根线，按 **Tab**，会选中这根线所在的整条走线；**再按 Tab**，选中整个网络(Net)。 |
| **S** + **L** | **选中线段** (Touching Line) | 选中被一条线划过的所有物体（类似CAD的划线选择）。 |
| **S** + **P** | **选中物理连接** (Physical) | 选中物理上连在一起的铜皮和线，哪怕它们不是同一个网络名。 |
| **S** + **C** | **选中区域内** (Inside Area) | 框选完全在框内的物体。 |
| **S** + **T** | **选中接触区域** (Touching Rect) | 框选只要碰到框边的物体（包含框内）。 |
| **x** + **A** | **取消所有选择** | 等同于点空白处，但在复杂板子里用快捷键更稳。 |

---

### 六、 布局与对齐 (Placement & Align)

除了 **A** 键调出对齐菜单外，还有几个移动时的骚操作：

* **M** (菜单)：移动菜单。
* **M** + **S**：**Move Selection**。先选中一堆电阻，按 M+S，可以一起移动，保持相对位置不变。


* **拖动时按 L**：**换层**。拖动元件时按 L，元件会自动翻转到背面（Bottom层），并自动镜像。
* **Ctrl** + **箭头键**：**微调位置**。按一次移动一个栅格单位。
* **Shift** + **Ctrl** + **箭头键**：**大步移动**。按一次移动 10 个栅格单位。

---

### 七、 “速查小抄”

为了方便复习，我把刚才补充的和之前最核心的，整理成一个**Layout 黄金流**：

1. **准备**：**Q** (切单位) -> **G** (设栅格) -> **D+R** (设规则)。
2. **布局**：**Space** (旋转) -> **L** (换层) -> **A** (对齐)。
3. **走线**：**P+T** (开始) -> **Shift+R** (推挤模式) -> **Shift+Space** (圆弧/折线) -> **BackSpace** (回退)。
4. **铺铜**：**P+G** (铺) -> **P+Y** (挖洞) -> **T+G+A** (全部重铺)。
5. **检查**：**Shift+S** (单层看) -> **T+D+R** (DRC检查)。

这次补充的这些，特别是 **Shift+R (推挤模式)** 和 **Tab (级联选择)** 以及 **铺铜管理(Shelve/Repour)**，是你从新手迈向熟练工必须要用的。还有哪里觉得不够细，继续喷，我继续补！
### 八、 几个特别的“坑” (复习专用)

这也是我之前容易卡住的地方，专门列出来：

1. **绿色报错满天飞**：
* 这是因为元件触犯了默认规则。比如两个焊盘太近，或者字符出了板框。
* **解决**：要么修改布局，要么去 `Design` -> `Rules` 里把限制改宽一点。T + M 可以复位错误标记。


2. **原理图更新到PCB没反应**：
* 检查是否给元件添加了 Footprint（封装）。
* 检查封装库路径是否正确。
* 检查原理图里的 Designator（标号）是否有重复。


3. **多边形铺铜 (Polygon Pour) 不显示**：
* 铺铜后如果是空的（只有框），右键选择该铜皮 -> `Polygon Actions` -> `Repour Selected`。
* 或者按快捷键 **T** + **G** + **A** (Repour All)。



---